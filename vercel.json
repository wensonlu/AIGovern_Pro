{
  "version": 2,
  "name": "ai-govern-pro-backend",
  "buildCommand": "pip install -r backend/requirements.txt",
  "outputDirectory": "backend",
  "routes": [
    { "src": "/(.*)", "dest": "/backend/api/index.py" }
  ],
  "env": {
    "PYTHONPATH": "/var/task/backend"
  }
}
